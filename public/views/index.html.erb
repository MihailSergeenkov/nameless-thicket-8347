<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="../css/style.css" type="text/css">
    <title>Игра в кости</title>
  </head>
  <body>
    <h1>Играем в кости!!!</h1>
    <% if not @login %>
      <h2>Добро пожаловать!</h2>
      <form method="post">
        <input placeholder="Ваше имя" name="name">
        <input type="submit" formaction="?action=login" value="Играть!">
      </form>
    <% else %>
      <h2><%= @player_name.capitalize %>, делай ставку!</h2>
      <% if @points %>
        <h2><%= "#{@message}" %></h2>
        <h2>У тебя <%= @points %> очков</h2>
      <% else %>
        <h2>Возникла ошибка, зайдите снова!</h2>
      <% end %>
      <form method="post">
        <input placeholder="Число" name="bet">
        <input placeholder="Ставка" name="stake">
        <input type="submit" formaction="?action=turn" value="Поехали!">
      </form>
      <br>
      <form action="?action=logout" method="post">
        <button>Выйти...</button>
      </form>
    <% end %>
  </body>
</html>